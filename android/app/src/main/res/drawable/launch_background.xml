<?xml version="1.0" encoding="utf-8"?>
<!-- Shown until Flutter renders its first frame. Solid color matches
     the app's dark background (#111827), the same fallback used behind
     the animated gradient in bg.css / theme-color meta tag. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
</layer-list>
